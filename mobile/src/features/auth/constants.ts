/**
 * Version of the Terms + Privacy Policy a user agrees to at sign-up. MUST stay in sync
 * with `frontend/src/pages/Auth.jsx`'s `REGISTRATION_POLICY_VERSION` — this is a shared,
 * auditable consent record (Golden Rule #5), not a mobile-only value. Bump both together
 * when either legal document materially changes.
 */
export const REGISTRATION_POLICY_VERSION = '2026-03-01';
